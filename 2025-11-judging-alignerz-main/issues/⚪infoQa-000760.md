# [000760] Unused `IAlignerzVesting` import
  
  ### Summary

The unused `IAlignerzVesting` import in `AlignerzVesting.sol` has no functional impact but adds dead code and minor maintenance noise as the interface is never referenced anywhere in the contract.

### Root Cause

In `AlignerzVesting.sol:12-18` the contract imports `IAlignerzVesting` but does not use it for any type declarations, function calls, or interface references, so the import is redundant.

```solidity
import {WhitelistManager} from "./whitelistManager/WhitelistManager.sol";
import {FeesManager} from "./feesManager/FeesManager.sol";
import {IAlignerzNFT} from "../../interfaces/IAlignerzNFT.sol";
>> import {IAlignerzVesting} from "../../interfaces/IAlignerzVesting.sol"; 

/// @title AlignerzVesting - A vesting contract for token sales
/// @notice This contract manages token vesting schedules for multiple projects
contract AlignerzVesting is Initializable, UUPSUpgradeable, OwnableUpgradeable, WhitelistManager, FeesManager {
```

### Internal Pre-conditions

Not applicable

### External Pre-conditions

Not applicable

### Attack Path

Not applicable

### Impact

No security or economic impact; it is purely an informational/code hygiene issue.

### PoC

Not applicable

### Mitigation

Remove the unused import line from `AlignerzVesting.sol` to keep the contract dependencies minimal and accurate:

```diff
- import {IAlignerzVesting} from "../../interfaces/IAlignerzVesting.sol";
```
  